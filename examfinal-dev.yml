spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect
  datasource:
      url: jdbc:mysql://localhost:3306/proyectofinal?useSSL=false&serverTimezone=America/Lima
      username: root
      password: angelo
      driver-class-name: com.mysql.cj.jdbc.Driver
    
